import { ChartsBackgroundDirective } from './charts-background.directive';

describe('ChartsBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartsBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
